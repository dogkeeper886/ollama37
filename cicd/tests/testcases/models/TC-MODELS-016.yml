id: TC-MODELS-016
name: gemma4:12b Multimodal Inference
suite: models
priority: 4
timeout: 1200000
dependencies: []
issue: 367
intent:
  user_story: |
    Verify gemma4:12b answers text, image and audio prompts on the K80

steps:
  - name: Warm up (tolerate transient cold-load retry under suite GPU pressure)
    command: |
      # ~10GB split across 2 K80 dies; under back-to-back-model GPU pressure the first cold load
      # can crash ("llama runner terminated") and ollama auto-retries. Absorb that here so the
      # real assertions below run against the loaded model.
      for i in 1 2 3 4 5; do
        r=$(curl -s http://localhost:11434/api/generate \
          -d '{"model":"gemma4:12b","prompt":"hi","stream":false,"options":{"num_predict":1}}')
        echo "$r" | grep -q '"response"' && { echo "WARM_OK"; break; }
        echo "warm attempt $i: $(echo "$r" | head -c 120)"; sleep 8
      done
    expectPatterns:
      - "WARM_OK"
    rejectPatterns:
      - "CUBLAS_STATUS"
      - "CUDA error"

  - name: Text inference
    command: |
      curl -s http://localhost:11434/api/generate \
        -d '{"model":"gemma4:12b","prompt":"What is 2+2? Answer briefly.","stream":false,"options":{"num_predict":64,"temperature":0,"seed":0}}' \
        | head -c 2000
    expectPatterns:
      - '"response":"[^"]*4'
    rejectPatterns:
      - "CUBLAS_STATUS"
      - "CUDA error"

  - name: GPU offload check (must be on GPU, not CPU)
    command: docker exec ollama37 nvidia-smi --query-compute-apps=pid,used_memory --format=csv
    expectPatterns:
      - "[0-9]+ MiB"

  - name: Vision inference
    command: |
      IMG=$(python3 - <<'PY'
      import base64, struct, zlib
      # 224x224 RGB: white disc on black (a simple, describable shape)
      w=h=224; px=bytearray()
      for y in range(h):
          px.append(0)  # PNG filter byte per row
          for x in range(w):
              d=((x-112)**2+(y-112)**2)**0.5
              v=255 if d<70 else 0
              px += bytes((v,v,v))
      def chunk(t,d):
          c=t+d; return struct.pack('>I',len(d))+c+struct.pack('>I',zlib.crc32(c)&0xffffffff)
      png=b'\x89PNG\r\n\x1a\n'+chunk(b'IHDR',struct.pack('>IIBBBBB',w,h,8,2,0,0,0))+chunk(b'IDAT',zlib.compress(bytes(px)))+chunk(b'IEND',b'')
      print(base64.b64encode(png).decode())
      PY
      )
      curl -s http://localhost:11434/api/chat \
        -d "{\"model\":\"gemma4:12b\",\"messages\":[{\"role\":\"user\",\"content\":\"What shape is in this image? Answer in one short sentence.\",\"images\":[\"$IMG\"]}],\"stream\":false,\"think\":false,\"options\":{\"num_predict\":64}}" \
        | head -c 2000
    expectPatterns:
      # the model perceives the white disc — any of these confirms vision works
      - "circle|disc|round|white|shape|oval|dot|sphere|black"
    rejectPatterns:
      - "CUBLAS_STATUS"
      - "CUDA error"
      - "does not support"
      - "cannot see|can't see|unable to see|no image"

  - name: Audio inference (spoken word)
    command: |
      espeak-ng -s 130 -w /tmp/tc016_audio.wav "hello hello hello" 2>/dev/null
      python3 -c "import base64,json; b=base64.b64encode(open('/tmp/tc016_audio.wav','rb').read()).decode(); json.dump({'model':'gemma4:12b','messages':[{'role':'user','content':'Is a single word repeated in this audio? Answer in one short sentence.','images':[b]}],'stream':False,'think':False,'options':{'num_predict':64}}, open('/tmp/tc016_req.json','w'))"
      curl -s http://localhost:11434/api/chat -d @/tmp/tc016_req.json | head -c 2000
    expectPatterns:
      # the model perceives + engages with the audio (transcription accuracy is a quality
      # metric, not a load gate — encoder-free fidelity is marginal on synthetic speech)
      - "word|repeat|audio|sound|three|times|phrase|hello|spoken"
    rejectPatterns:
      - "CUBLAS_STATUS"
      - "CUDA error"
      - "does not support audio"
      - "cannot hear|can't hear|text-based|text-only|no audio|don't have the ability"

  - name: Unload model
    command: |
      curl -s http://localhost:11434/api/generate \
        -d '{"model":"gemma4:12b","keep_alive":0}'
      echo "Model unloaded"
    expectPatterns:
      - "Model unloaded"

criteria: |
  Verify gemma4:12b answers on the K80 and gives its dies back:
  - Model loads (the warm-up shows "WARM_OK")
  - Answer contains a 4 (api/generate shows "response":"[^"]*4")
  - Weights are resident on the GPU (nvidia-smi shows "<N> MiB")
  - Image is described (api/chat shows the shape or its colour)
  - Audio is heard (api/chat shows the spoken word or that it repeats)
  - Model unloads (api/generate with keep_alive 0 shows "Model unloaded")
